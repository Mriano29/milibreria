{
    "compilerOptions": {
      "target": "ES5", // Transpila a ES5 para compatibilidad.
      "useDefineForClassFields": true, // Define campos de clases de forma más estricta.
      "lib": ["ES2020", "DOM", "DOM.Iterable"], // Bibliotecas disponibles para el proyecto.
      "module": "ESNext", // Usa módulos ESNext.
      "skipLibCheck": true, // Evita la verificación de archivos de declaración de bibliotecas.
      "moduleResolution": "bundler", // Resolución de módulos específica para bundlers como Vite.
      "allowImportingTsExtensions": true, // Permite importar archivos con extensión .ts.
      "resolveJsonModule": true, // Permite la importación de JSON.
      "isolatedModules": true, // Asegura que cada archivo se maneje como un módulo separado.
      "noEmit": true, // No genera archivos de salida.
      "jsx": "react-jsx", // Soporte JSX para React.
      "strict": true, // Activar verificación estricta de tipos.
      "noUnusedLocals": true, // Marca variables no utilizadas.
      "noUnusedParameters": true, // Marca parámetros no utilizados.
      "noFallthroughCasesInSwitch": true, // Requiere que todos los casos en switch se manejen.
      "declaration": true // Genera archivos de declaración.
    },
    "include": ["src"], // Incluir archivos en la carpeta src.
    "exclude": ["src/**/__docs__", "src/**/__test__"] // Excluir carpetas de pruebas y documentación.
  }
  